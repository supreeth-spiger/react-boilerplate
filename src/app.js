import React from 'react';
import ReactDOM from 'react-dom';

import "./app.css";

const Starter = () => <h1>Greeting's from React BoilerPlate</h1>;

ReactDOM.render(<Starter />, document.getElementById('root'));
